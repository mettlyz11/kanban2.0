          
          {/* 图表区域 */}
          {llmData.daily_stats && llmData.daily_stats.length > 0 && (
            <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: 16, marginBottom: 24 }}>
              <h3 style={{ marginBottom: 12 }}>📈 每日Token使用趋势</h3>
              <div style={{ height: 300 }}>
                <Line
                  data={{
                    labels: llmData.daily_stats.map(s => s.date).reverse(),
                    datasets: [
                      {
                        label: 'Token数',
                        data: llmData.daily_stats.map(s => s.total_tokens).reverse(),
                        borderColor: '#3b82f6',
                        backgroundColor: 'rgba(59, 130, 246, 0.1)',
                        tension: 0.4,
                        fill: true
                      },
                      {
                        label: '调用次数',
                        data: llmData.daily_stats.map(s => s.total_calls * 1000).reverse(),
                        borderColor: '#10b981',
                        backgroundColor: 'rgba(16, 185, 129, 0.1)',
                        tension: 0.4,
                        yAxisID: 'y1'
                      }
                    ]
                  }}
                  options={{
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                      y: {
                        beginAtZero: true,
                        title: { display: true, text: 'Token数' }
                      },
                      y1: {
                        beginAtZero: true,
                        position: 'right',
                        title: { display: true, text: '调用次数 (×1000)' },
                        grid: { drawOnChartArea: false }
                      }
                    }
                  }}
                />
              </div>
            </div>
          )}
          
          {llmData.token_stats && llmData.token_stats.length > 0 && (
            <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: 16, marginBottom: 24 }}>
              <h3 style={{ marginBottom: 12 }}>📊 Provider模型使用占比</h3>
              <div style={{ height: 300 }}>
                <Bar
                  data={{
                    labels: llmData.token_stats.slice(0, 5).map(s => s.provider + '\n' + s.model),
                    datasets: [
                      {
                        label: 'Token总数',
                        data: llmData.token_stats.slice(0, 5).map(s => s.total_tokens),
                        backgroundColor: '#3b82f6',
                      },
                      {
                        label: '总成本($)',
                        data: llmData.token_stats.slice(0, 5).map(s => s.total_cost * 10000),
                        backgroundColor: '#f59e0b',
                        yAxisID: 'y1'
                      }
                    ]
                  }}
                  options={{
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                      y: {
                        beginAtZero: true,
                        title: { display: true, text: 'Token总数' }
                      },
                      y1: {
                        beginAtZero: true,
                        position: 'right',
                        title: { display: true, text: '总成本 ($ × 10000)' },
                        grid: { drawOnChartArea: false }
                      }
                    }
                  }}
                />
              </div>
            </div>
          )}
          
          {llmData.actor_stats && llmData.actor_stats.length > 0 && (
            <div style={{ background: '#fff', border: '1px solid #e5e7eb', borderRadius: 8, padding: 16, marginBottom: 24 }}>
              <h3 style={{ marginBottom: 12 }}>🥧 模块调用占比</h3>
              <div style={{ height: 300, display: 'flex', justifyContent: 'center', alignItems: 'center' }}>
                <Pie
                  data={{
                    labels: llmData.actor_stats.slice(0, 6).map(s => s.source),
                    datasets: [
                      {
                        data: llmData.actor_stats.slice(0, 6).map(s => s.total_calls),
                        backgroundColor: [
                          '#3b82f6', '#10b981', '#f59e0b', '#ef4444', '#8b5cf6', '#ec4899'
                        ],
                        borderWidth: 1
                      }
                    ]
                  }}
                  options={{
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                      legend: { position: 'right' }
                    }
                  }}
                />
              </div>
            </div>
          )}
